class Hexagram {
  String? name;
  String? hex;
  int? gate,
      line,
      color,
      tone,
      base;

  Hexagram({this.name, this.hex,this.gate, this.line, this.color, this.tone, this.base});
}